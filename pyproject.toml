[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "PairwiseAlignment"
version = "0.1.0"
dependencies = [
    "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]
